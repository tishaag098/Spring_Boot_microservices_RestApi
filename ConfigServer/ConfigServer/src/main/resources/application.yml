server:
  port: 8083
  
spring:
  application:
    name: CONFIG-SERVER
    
  cloud:
    config:
      server:
        git:
          uri:  https://github.com/tishaag098/microservice-config
          clone-on-start:  true
  